# MDXテストページ

このページはMDXが正しく動作しているかをテストするためのページです。

## テスト項目

- ✅ MDXファイルが正しくレンダリングされる
- ✅ Markdownの構文が正しく表示される
- ✅ コードブロックが表示される

```typescript
interface TestProps {
  message: string;
}

const TestComponent: React.FC<TestProps> = ({ message }) => {
  return <div>{message}</div>;
};
```

## 動作確認

MDXセットアップが正常に完了しました！